# vendor/jsqr

ログイン画面の「QR コードを読み取る」（カメラ映像から合言葉の QR を読む）に使う。
`qr-scan.js` が押されたときだけ `<script>` で読む（UMD。precache には入れるが起動時には読まない）。

| ファイル | 出所 | バージョン | ライセンス |
|---|---|---|---|
| `jsQR.js` | npm `jsqr` の `dist/jsQR.js` | 1.4.0 | Apache-2.0（`LICENSE`） |
