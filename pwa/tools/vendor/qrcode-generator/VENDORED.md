# vendor/qrcode-generator

`make-config.mjs` がログイン用 QR（SVG）を作るのに使う。配信物ではない（`pwa/tools/` は Pages に載せない）。

| ファイル | 出所 | バージョン | ライセンス |
|---|---|---|---|
| `qrcode.mjs` | npm `qrcode-generator` の `dist/qrcode.mjs`（Kazuhiko Arase） | 2.0.4 | MIT |
